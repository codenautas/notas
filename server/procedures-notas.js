"use strict";

var bestGlobals=require('best-globals');

var ProceduresNotas = {};


ProceduresNotas = [
    /*{
        action:'',
        parameters:[
            {name:''     , typeName:'' , label:''},
        ],
        coreFunction:function(context, parameters){
            return context.client.query(
               ,
                [parameters.]
            ).fetchAll().then(function(result){
                return JSON.stringify(result.rows);
            });
        }
    },*/
];

module.exports = ProceduresNotas;